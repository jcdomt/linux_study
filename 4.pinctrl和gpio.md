### `Pinctrl` 和 `GPIO` 子系统

 用于管脚控制和通用输入输出引脚的控制

原来的引脚控制方法：直接控制寄存器

```C
if (kbuf[0] == '1' || kbuf[0] == 1) {
	*vir_gpio_dr |= (1 << 5); // 设置 GPIO5 输出高电平
} else if (kbuf[0] == '0' || kbuf[0] == 0) {
	*vir_gpio_dr &= ~(1 << 5); // 设置 GPIO5 输出低电平
} else {
	return -EINVAL; // 无效参数
}
```

#### `Pinctrl` 子系统

1. 管理系统中所有可控制的 pin，在系统初始化时，枚举所有可控制的 pin，并标识这些 pin

2. 管理这些 pin 的复用。对于 SOC 而言，其引脚除了可配置为普通的 GPIO，若干个引脚还可以组成一个 pin group，形成特定的功能

   ```C
   i2c5 {
   		/omit-if-no-ref/
   		i2c5m0_xfer: i2c5m0-xfer {
   			rockchip,pins =
   				/* i2c5_sclm0 */
   				<3 RK_PB3 4 &pcfg_pull_none_smt>,
   				/* i2c5_sdam0 */
   				<3 RK_PB4 4 &pcfg_pull_none_smt>;
   		};
   
   		/omit-if-no-ref/
   		i2c5m1_xfer: i2c5m1-xfer {
   			rockchip,pins =
   				/* i2c5_sclm1 */
   				<4 RK_PC7 2 &pcfg_pull_none_smt>,
   				/* i2c5_sdam1 */
   				<4 RK_PD0 2 &pcfg_pull_none_smt>;
   		};
   	};
   ```

   

3. 配置这些 pin 的特性，例如使能或关闭引脚上的 pull-up、pull-down 电阻，配置其引脚的 driver strength

4. 可以通过`kernel/Documentation/devicetree/bindings`中的文档查看 `pinctrl`

5. 如何在代码中使用`pin controller`中定义的节点

   ```C
   # 设备的状态，可以有多个状态，default 为状态0
   pinctrl-names = "default";
   # 第0个状态所对应的引脚配置，也就是 default 状态对饮的引脚在 pin controller 里面定义好的节点 pinctrl_hog_1 里面的管脚配置
   pinctrl-0 = <&pinctrl_hog_1>;
   ```

   ```C
   # 两种状态
   pinctrl-names = "default", "wake up";
   pinctrl-0 = <&pinctrl_hog_1>;
   pinctrl-1 = <&pinctrl_hog2>;
   ```

   ```C
   pinctrl-names = "default";
   # default 对应的引脚在 pin contrller 里面定义好的节点，对应 pinctrl_hog_1 和 pinctrl_hog_2
   pinctrl-0 = <&pinctrl_hog_1 &pinctrl_hog_2>;
   ```

6. 统一抽象出的模型

   ```C
   &pinctrl {
       xxx {
           xxx_pins: xxx-pins {
               rockchip,pins =
                   <...>;
           };
       };
   };
   
   &device {
       pinctrl-names = "default", "sleep";
       pinctrl-0 = <&xxx_pins>;
       pinctrl-1 = <&xxx_sleep>;
   };
   ```

   ```
                       SoC
                        │
                ┌───────┴────────┐
                │                │
          Pin Controller       外设控制器
                │                │
                │                │
          ┌─────┴─────┐          │
          │           │          │
         Pin         Pin         I2C5
          │           │          │
          └─────┬─────┘          │
                │                │
             pinmux ─────────────┘
                │
                ▼
           “这些 Pin 给 I2C5”
                │
                ▼
          pinconf 配置
          pull/drive/...
   ```



---

#### `GPIO` 子系统

当 `Pinctrl`将一个引脚设置为`GPIO`

##### 设备树使用`Pinctrl`和`GPIO`子系统描述一个`GPIO`

```C
test1:test {
	#address-cells = <1>;
	#size-cells = <1>;
	
	compatible = "test";
	reg = <0x20ac000 0x0000004>;
	
	pinctrl-names = "default";
	pinctrl-0 = <&pinctrl-test>;
	test-gpio = <&gpio1 3 GPIO_ACTIVE_LOW>;
}
```

##### 常用的 `GPIO`函数

1. `of_get_named_gpio`函数

   用于获取 GPIO 编号，因为 Linux 内核中关于 GPIO 的 API 函数都要使用 GPIO 编号，此函数会将设备树种类似 `<&gpio1 3 GPIO_ACTIVE_LOW>`的信息转换为对应的 `GPIO`编号

   ```C
   // np: 设备节点
   // propname: 包含要获取 GPIO 信息的属性名
   // index: 一个属性可能包含多个 GPIO，用于指定获取哪个；如果只有一个则为 0
   int gpio = of_get_named_gpio(struct device_node *np, const char *propname, int index);
   // 返回获取到的 gpio 编号
   // 获取失败则返回负值
   ```

2. `gpio_request`函数

   用于申请一个 `GPIO` 管脚

   ```C
   // gpio: 要申请的 gpio 标号，使用 of_get_named_gpio 函数从设备树获取指定 GPIO 属性信息，次函数会返回这个 GPIO 的标号
   // lable: 给这个 GPIO 设定的名字
   int ret = gpio_request(unsigned gpio, const char *label);
   // 0 申请成功，否则申请失败
   ```

3. `gpio_free`函数

   用于释放一个 `GPIO`编号

   ```C
   gpio_free(unsigned gpio);
   ```

4. `gpio_direction_input`函数

   设置某个 `GPIO` 为输入

   ```C
   int ret = gpio_direction_input(unsigned gpio);
   ```

5. `gpio_direction_output`函数

   设置某个`GPIO`为输出

   ```C
   // value: 默认输出值，0/1
   int ret = gpio_direction_output(unsigned gpio, int value);
   ```

6. `gpio_get_value`函数

   用于获取某个`GPIO`的值

   ```C
   int ret = gpio_get_value(unsigned gpio);
   // 返回 0 或 1，失败则返回负值
   ```

7. `gpio_set_value`函数

   用于设置一个 `GPIO`的值

   ```C
   gpio_set_value(unsigned gpio, int value);
   ```

 

---

#### 实践

##### 编写设备树

```C
// 这是 RockChip 中定义的 GPIO3 控制器
gpio3: gpio@fe760000 {
	compatible = "rockchip,gpio-bank";
	reg = <0x0 0xfe760000 0x0 0x100>;
	interrupts = <GIC_SPI 36 IRQ_TYPE_LEVEL_HIGH>;
	clocks = <&cru PCLK_GPIO3>, <&cru DBCLK_GPIO3>;
	gpio-controller;
	#gpio-cells = <2>;
	interrupt-controller;
	#interrupt-cells = <2>;
};


test1:test {
	#address-cells = <1>;
	#size-cells = <1>;
	
	compatible = "test";
	// 对于操作 GPIO，则使用 gpio 号就够了，不需要使用
	//reg = <0x20ac000 0x0000004>;
	
	pinctrl-names = "default";
	// 使用 beep 的节点信息充当 test1 的 default pinctrl组
	pinctrl-0 = <&pinctrl_beep>;
	//
	// 给 of_get_named_gpio 查找用的属性值
	// 引用 GPIO3 寄存器的 RK_PA5口，定义高电压为1
	//
	// 值得注意的是，这串 beep-gpios 类似于函数调用
	// gpio3 中存在定义 #gpio-cells = <2>;
	// 意思就是这个 gpio-controller 接受两个参数
	beep-gpios = <&gpio3 RK_PA5 GPIO_ACTIVE_HIGH>;
	
	status = "okay";
};

&pinctrl {
	pinctrl_beep:beep {
		// RK 的引脚定义是 <Bank Pin Mux Config>
		// 假设使用 GPIO3_A5 口
		// pcfg_pull_none_smt 表示不启用上下拉电阻0
		// pcfg_pull_down_smt 表示启用下拉电阻，使 GPIO 表现为 0
		// pcfg_pull_up_smt   表示启用上拉电阻，是 GPIO 表现为 1
		// smt 为施密特触发器，用于稳定信号，可以不管启用就行
		rockchip,pins = <
			3 RK_PA5 0 &pcfg_pull_none_smt
		>;
	};
};
```

![image-20260825151858605](.\assets\image-20260825151858605.png)

- **注意：需要保证一个引脚在设备树中只有一个定义**

##### 引用设备树中的 GPIO

1. 使用 `pdev->dev.node`或`of_find_node_by_path`获取设备节点
2. 使用 `of_get_named_gpio`函数获取 `GPIO`编号
3. 使用`gpio_request`请求`GPIO`资源
4. 使用 `GPIO`
5. 注册杂项设备辅助使用
6. 注意如果函数错误结束，应该释放资源

```c
#include <linux/module.h>
#include <linux/of.h>
#include <linux/platform_device.h>
#include <linux/of_gpio.h>
#include <linux/miscdevice.h>

MODULE_LICENSE("GPL");

static unsigned global_beep_gpio; // 全局变量，用于存储 GPIO 号

const struct of_device_id beep_of_match[] = {
    {.compatible = "test"},
    {}
};

static ssize_t beep_write(struct file *file, const char __user *buf, size_t count, loff_t *ppos)
{
    // 这里假设你有一个写操作函数，用于控制 GPIO
    // 例如，根据用户传入的数据来设置 GPIO 的高低电平
    char kbuf[255];
    if (copy_from_user(kbuf, buf, sizeof(kbuf))) {
        return -EFAULT;
    }

    if (kbuf[0] == '1') {
        gpio_set_value(global_beep_gpio, 1); // 设置为高电平
    } else if (kbuf[0] == '0') {
        gpio_set_value(global_beep_gpio, 0); // 设置为低电平
    } else {
        return -EINVAL; // 无效参数
    }

    return count;
}

struct file_operations beep_fops = {
    .owner = THIS_MODULE,
    .write = beep_write,
};

struct miscdevice beep_misc_device = {
    .minor = MISC_DYNAMIC_MINOR,
    .name = "beep",
    .fops = &beep_fops,
};

static int beep_probe(struct platform_device *pdev)
{
    int func_ret = 0;

    printk(KERN_INFO "Beep device probed\n");

    // 获取设备节点
    struct device_node *test_device_node = pdev->dev.of_node;
    if (test_device_node == NULL) {
        printk(KERN_ERR "Failed to get device node\n");
        func_ret = -ENODEV;
        goto error_exit;
    }

    // 从设备节点中获取 GPIO
    int beep_gpio = of_get_named_gpio(test_device_node, "beep-gpios", 0);
    if (beep_gpio < 0) {
        printk(KERN_ERR "Failed to get beep-gpio\n");
        func_ret = beep_gpio;
        goto error_exit;
    }
    global_beep_gpio = beep_gpio;

    // 请求 GPIO 资源
    int ret = gpio_request(beep_gpio, "beep");
    if (ret) {
        printk(KERN_ERR "Failed to request beep_gpio\n");
        func_ret = ret;
        goto error_exit;
    }

    // 使用 GPIO
    gpio_direction_output(beep_gpio, 0); // 设置为输出，初始值为低电平
    gpio_set_value(beep_gpio, 1); // 设置为高电平

    // 注册杂项设备等用于操作 GPIO
    ret = misc_register(&beep_misc_device);
    if (ret) {
        printk(KERN_ERR "Failed to register beep misc device\n");
        func_ret = ret;
        goto error_exit;
    }
    // ...

    return 0;

error_exit:
    misc_deregister(&beep_misc_device);
    gpio_free(beep_gpio);
    global_beep_gpio = 0;

    return func_ret;
}

static int beep_remove(struct platform_device *pdev)
{
    printk(KERN_INFO "Beep device removed\n");

    // 注销杂项设备
    misc_deregister(&beep_misc_device);

    // 释放 GPIO 资源
    gpio_free(global_beep_gpio);
    global_beep_gpio = 0;

    return 0;
}

struct platform_driver beep_driver = {
    .probe = beep_probe,
    .remove = beep_remove,
    .driver = {
        .name = "beep",
        .of_match_table = beep_of_match,
    },
};

module_platform_driver(beep_driver);
```



