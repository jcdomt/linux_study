### 设备树

描述硬件资源，通过 `Bootlaoder` 将硬件资源传给内核。

将平台总线模型的 `device.c` 变成设备树文件  

#### 基本概念

- **DT**：设备树
- **FDT**：展开设备树
- **DTS**：设备树源码
- **DTSI**：更通用的设备树代码
- **DTB**：`DTS`编译后得到的
- **DTC**：设备树编译器

#### 设备树的基本语法

- 设备树从根节点开始，每个设备都是一个节点
- 节点和节点之间可以相互嵌套，形成父子关系
- 设备的属性用 `KV对` 来描述。每个属性用分号结束  

##### 根节点

```DTS
/{

};
```

##### 子节点

```DTS
/{
	node1
	{
		child-node1
		{
		
		};
	};
	node2
	{
	
	};
}
```

  ##### 节点名称

```DTS
[别名:] <名称>[@设备地址]
uart8: serial@02288000
```

##### 节点的引用

一般往一个节点中添加内容的时候，不会直接把添加的内容写到节点里面，而是通过节点的引用来添加

```DTS
&uart8 {
	pinctrl-names = "default";
	pinctrl-0 = <&pinctrl_uart8>;
	status = "okay";
};
```

就是向已经定义过的 `uart8`追加内容

#####  属性

1. reg 属性

   ```DTS
   reg = <add1 length1 [add2 length2]>;
   ```

2. #address-cells 和 #size-cells 属性

   - #address-cells 用来设置子节点中 reg 地址的数量`
   - #size-cells 用来设置子节点中 reg 地址的长度

3. compatible 属性

   是一个字符串列表，可以在代码中进行匹配

4. status 属性

   `okay` 或 `disable`

```DTS
cpu {
	#address-cells = <1>;
	#size-cells = <1>;
	
	serial@02288000 {
		compatible = "serial"; 
		// 从 101F2000 为起始地址，长度为 0x1000
		reg = <101F2000 0x1000>;
		status = "okay";
	};
};
```

#### 设备树的编写

```DTS
test1:test {
	compatible = "test";
	#address-cells = <1>;
	#size-cells = <1>;
	reg = <0x20ac000 0x0000004>;
	
	status = "disable";
}

&test1 {
	status = "okay";
}
```

##### 编译

```shell
apt-get install device-tree-compiler
```

然后进入内核源码目录使用

```shell
make ARCH=arm64 CROSS_COMPILE=aarch64-none-linux-gnu- dtbs
make ARCH=arm64 CROSS_COMPILE=aarch64-none-linux-gnu- xxx.dtb
```

查看系统中添加了哪些节点：`ls /proc/device-tree/`

- **注意：我们一般不自己从头编写一个完整的`dts`文件，而是修改 SOC 厂商提供的文件**

---



### 在驱动中去读设备树信息

#### 设备节点

Linux 使用 `device_node` 结构体来描述一个节点，在节点的属性信息中保存了驱动法所需要的内容

```C
struct device_node {
	const char *name;
	phandle phandle;
	const char *full_name;
	struct fwnode_handle fwnode;

	struct	property *properties;	// 设备节点的属性
	struct	property *deadprops;	/* removed properties */
	struct	device_node *parent;
	struct	device_node *child;
	struct	device_node *sibling;
#if defined(CONFIG_OF_KOBJ)
	struct	kobject kobj;
#endif
	unsigned long _flags;
	void	*data;
#if defined(CONFIG_SPARC)
	unsigned int unique_id;
	struct of_irq_controller *irq_trans;
#endif
};

struct property {	// 在 DTS 中设定的键值对
	char	*name;
	int	length;
	void	*value;
	struct property *next;
#if defined(CONFIG_OF_DYNAMIC) || defined(CONFIG_SPARC)
	unsigned long _flags;
#endif
#if defined(CONFIG_OF_PROMTREE)
	unsigned int unique_id;
#endif
#if defined(CONFIG_OF_KOBJ)
	struct bin_attribute attr;
#endif
};
```

#### of 函数

##### 查找节点的 of 函数、

1. 通过路径来查找指定的节点

   ```C
   // path: 带有全路径的节点名，可石永红节点的别名
   of_find_node_by_path(const char *path);
   struct device_node *backlightNode = of_find_node_by_path("/screen/backlight");
   ```

2. 获取指定节点的父节点

   ```C
   of_get_parent(const struct device_node *node);
   struct device_node *screenNode = of_get_parent(backlightNode);
   ```

3. 查看一个设备节点的下一个兄弟节点

   ```C
   // node: 父节点
   // prev: 前一个子节点，也就是从哪一个子节点开始迭代的查找下一个字节点，可以设置为 NULL，表示获取第一个子节点
   of_get_next_child(const device_node *node, const device_node *prev);
   struct device_node *nextNode = of_get_next_child(screenNode, backlightNode);
   ```

   

##### 查找节点属性的 of 函数

1. 查看指定的属性

   ```C
   // np: 设备节点
   // name: 属性名字
   // lenp: 属性的字节数，这是内核返回的参数
   of_find_property(const struct device_node *np, const char *name, int *lenp);
   
   int len;
   struct property *prop = of_find_property(backlightNode, "compatible", &len);
   // 此时 prop 是 backlightNode 的 compatible 属性
   // len 是 该属性值的长度，*lenp == prop->length
   ```

2. 读取整形值属性

   ```C
   int of_property_read_u8(const struct *np, const char *name, u8 *out_value);
   int of_property_read_u16(const struct *np, const char *name, u16 *out_value);
   int of_property_read_u32(const struct *np, const char *name, u32 *out_value);
   int of_property_read_u64(const struct *np, const char *name, u64 *out_value);
   
   u32 light;
   int ret = of_property_read_u32(backlightNode, "light", &light);
   // 此时 ret 为 是否成功（0 为成功，负值为失败）
   // light 为读取到的 u32 值
   ```

3. 读取整数数组属性

   ```C
   // sz: 要读取的数组元素的数量
   int of_property_read_u8_array(const struct *np, const char *name, u8 *out_value, size_t sz);
   int of_property_read_u16_array(const struct *np, const char *name, u16 *out_value, size_t sz);
   int of_property_read_u32_array(const struct *np, const char *name, u32 *out_value, size_t sz);
   int of_property_read_u64_array(const struct *np, const char *name, u64 *out_value, size_t sz);
   
   u32 reg[2];
   of_property_read_u32_array(backlightNode, "reg", &reg, 2);
   // 此时 ret 为 是否成功（0 为成功，负值为失败）
   // reg 为读取到的 u32 数组
   ```

4. 读取字符串属性

   ```C
   of_property_read_string(struct device_node *np, const char *name, const char **outstring);
   
   char *status;
   int ret = of_property_read_string(backlightNode, "status", &status);
   ```

##### 内存操作函数

1. 直接内存映射

   使用 `of_iomap`函数用于直接内存映射，替代 `ioremap`函数完成物理地址到虚拟地址的映射

   ```C
   // np: 设备节点
   // index: reg 属性中药完成内存映射的短，如果 reg 属性只有一段则 index 为 0
   void *ret = of_iomap(struct device_node *np, int index);
   // 返回经过内存映射的虚拟内存的首地址，如果是 NULL 的话表示内存映射失败
   ```

   

### platform模型下的设备树驱动开发

在 `platform_driver`结构体中，补充 `.driver.of_match_table`，用于匹配设备树节点

```c
const struct of_device_id of_match_table_test = {
    {.compatible = "test" },
    {}
};

const struct platform_device_id beep_id_test = {
    .name = "test"
};

struct platform_driver beep_driver {
    .probe = beep_probe,
    .remove = beep_remove,
    .driver = {
        .owner = THIS_MODULE,
        .name = "test",								// 最后匹配
        .of_match_table = of_match_table_test,		// 优先匹配
    },
    .id_table = &beep_id_table						// 其次匹配
}
```

```C
#include <linux/module.h>
#include <linux/of.h>
#include <linux/platform_device.h>
#include <linux/of_address.h>

MODULE_LICENSE("GPL");

/*
beep:test {
	compatible = "test";
	#address-cells = <1>;
	#size-cells = <1>;
	reg = <0x20ac000 0x0000004>;
	
	status = "disable";
}

&beep {
	status = "okay";
}
*/

const struct of_device_id beep_of_match[] = {
    {.compatible = "test"},
    {}
};

unsigned long *beep_base_addr;

static int beep_probe(struct platform_device *pdev)
{
    printk(KERN_INFO "Beep device probed\n");

    // 获取设备树节点的资源
    // 匹配出来的资源存在于 pdev->dev.of_node 中
    printk(KERN_INFO "Device name: %s\n", pdev->dev.of_node->name);

    // 主流方法：通过查看设备树节点来实现功能
    struct device_node *test_device_node = pdev->dev.of_node;
    // 获取 reg 属性
    u32 reg_value[2];
    int ret = of_property_read_u32_array(test_device_node, "reg", reg_value, 2);
    if (ret) {
        printk(KERN_ERR "Failed to read reg property\n");
        return ret;
    }
    printk(KERN_INFO "Reg values: 0x%x, 0x%x\n", reg_value[0], reg_value[1]);

    // 映射 reg 的内存
    beep_base_addr = of_iomap(test_device_node, 0);
    if (!beep_base_addr) {
        printk(KERN_ERR "Failed to map reg memory\n");
        return -ENOMEM;
    }
    
    // 注册杂项设备等用于操作地址
    // ...

    return 0;
}

struct platform_driver beep_driver = {
    .driver = {
        .owner = THIS_MODULE,
        .name = "beep",
        .of_match_table = beep_of_match,
    },
    .probe = beep_probe,
};

module_platform_driver(beep_driver);
```

