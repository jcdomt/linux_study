### 等待队列

#### 阻塞与非阻塞

- 阻塞：当前设备如果不可读或不可写，也就是得不到资源的时候，那么当前从进程会被挂起。只有当设备满足条件的时候才可以返回。默认情况下，文件都是以这种方式打开。

- 非阻塞：当前设备不可读或不可写事，该函数不会阻塞当前进程，要么放弃，要么不停的查询，知道可以操作为止。

- ```C
  int fd = open(filepath, O_RDWR);	// 阻塞
  int fd = open(filepath, O_RDWR | O_NONBLOCK);	// 非阻塞
  ```

#### 等待队列基础知识

当进程去访问设备的时候，经常需要等待特定事件发生之后才能继续往下运行，这个时候就需要在驱动中实现当条件不满足时进程休眠，当条件满足时由内核唤醒进程。那么等待队列就实现了在事件上的等待条件。

##### 等待队列头

等待事件头就是一个等待队列的头部，每个访问设备的进程就是一个队列项，当设备不可用的时候就要将这些进程对应的等待队列项添加到等待队列里面。

比如用户态程序需要知晓硬件按键的状态，如果一直向驱动读取，则会占用大量`CPU`，于是可以使用等待队列，在驱动中阻塞用户进程，当按键按下时再唤醒

等待队列头使用结构体`wait_queue_head_t`表示，这个结构体定义在文件`include/linux/wait.h`中

```C
struct wait_queue_head {
	spinlock_t		lock;
	struct list_head	head;
};
typedef struct wait_queue_head wait_queue_head_t;
```

定义并初始化一个等待队列头

```C
wait_queue_head_t test_wq;
init_waitqueue_head(&test_wq);
/****************************************************/
// 也可以使用宏一次性完成等待队列头的定义和初始化
DECLARE_WAIT_QUEUE_HEAD(test_wq)
```

##### 等待队列相关函数

###### 1. `init_waitqueue_head`宏

动态初始化等待队列头结构

原型：

```C
// q: 指向等待队列头的指针
void init_waitqueue_head(wait_queue_head_t *q);
```

###### 2. `wait_event`宏

不可中断的阻塞等待，让调用进程进入不可中断的睡眠状态，在等待队列里面睡眠直到 `condition` 变为真，被内核唤醒

```C
// wq: 等待队列头
// condition: 等待的条件，为假时才会进入休眠
wait_event(wq, condition)
```

###### 3. `wait_event_interruptible`宏

可中断的阻塞等待，让调用进程进入可中断的睡眠状态，知道`condition`为真后被内核唤醒，或者被信号打断唤醒

```C
// wq: 等待队列头
// condition: 条件
wait_event_interruptible(wq, condition)
```

###### 4. `wake_up`宏

唤醒所有休眠进程

```C
// x: 等待队列头结构指针
wake_up(x)
```

###### 5. `wake_up_interruptible(x)`

唤醒可中断的休眠进程

```C
// x: 等待队列头的指针
wake_up_interruptible(x)
```

##### 编程实现

```C
#include <linux/module.h>

#include <linux/platform_device.h>
#include <linux/of.h>
#include <linux/of_gpio.h>

#include <linux/interrupt.h>

#include <linux/wait.h>
#include <linux/sched.h>

#include <linux/miscdevice.h>

MODULE_LICENSE("GPL");

int global_key_gpio = -1;
int global_irq = -1;

static int key_status = 0;

// 等待队列相关 ***************************************************************************************
DECLARE_WAIT_QUEUE_HEAD(key_wait_queue);


// 杂项设备相关 ***************************************************************************************
ssize_t key_read(struct file *file, char __user *buf, size_t count, loff_t *ppos)
{
    char status = key_status + '0'; // 将状态转换为字符 '0' 或 '1'

    // 启用等待队列
    wait_event_interruptible(key_wait_queue, key_status != 0); // 等待按键状态改变

    if (copy_to_user(buf, &status, 1)) {
        return -EFAULT;
    }
    return 1; // 返回读取的字节数
}

ssize_t key_write(struct file *file, const char __user *buf, size_t count, loff_t *ppos)
{
    char status;
    if (copy_from_user(&status, buf, 1)) {
        return -EFAULT;
    }
    if (status == '0') {
        key_status = 0;
    } else if (status == '1') {
        key_status = 1;
    } else {
        return -EINVAL; // 无效的输入
    }
    return 1; // 返回写入的字节数
}

struct file_operations key_fops = {
    .owner = THIS_MODULE,
    .read = key_read,
    .write = key_write,
};

struct miscdevice key_misc_device = {
    .minor = MISC_DYNAMIC_MINOR,
    .name = "key",
    .fops = &key_fops,
};

// 中断相关 ******************************************************************************************
irqreturn_t key_irq_handler(int irq, void *dev_id)
{
    key_status = 1 - key_status; // 切换按键状态
    printk(KERN_INFO "Key IRQ triggered, status: %d\n", key_status);

    // 在这里唤醒等待队列中的进程
    // 重新判定条件
    wake_up_interruptible(&key_wait_queue); // 唤醒等待队列
    
    return IRQ_HANDLED;
}

// platform 驱动相关 **********************************************************************************
int key_probe(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device probed\n");

    struct device_node *node = pdev->dev.of_node;
    if (!node) {
        printk(KERN_ERR "Failed to get device node\n");
        return -ENODEV;
    }

    // 获取 GPIO 资源
    int key_gpio = of_get_named_gpio(node, "key-gpios", 0);
    if (key_gpio < 0) {
        printk(KERN_ERR "Failed to get key-gpio\n");
        return key_gpio;
    }
    int ret = gpio_request(key_gpio, "key_gpio");
    if (ret) {
        printk(KERN_ERR "Failed to request key-gpio\n");
        return ret;
    }

    gpio_direction_input(key_gpio);
    gpio_set_debounce(key_gpio, 200); // 设置消抖时间为 200ms

    int irq = gpio_to_irq(key_gpio);
    if (irq < 0) {
        printk(KERN_ERR "Failed to get irq for key-gpio\n");
        gpio_free(key_gpio);
        return irq;
    }
    ret = request_irq(irq, key_irq_handler, IRQF_TRIGGER_FALLING | IRQF_TRIGGER_RISING, "key_irq", pdev);
    if (ret) {
        printk(KERN_ERR "Failed to request irq for key-gpio\n");
        gpio_free(key_gpio);
        return ret;
    }
    global_key_gpio = key_gpio;
    global_irq = irq;

    // 注册杂项设备
    ret = misc_register(&key_misc_device);
    if (ret) {
        printk(KERN_ERR "Failed to register misc device\n");
        free_irq(irq, pdev);
        gpio_free(key_gpio);
        return ret;
    }

    return 0;
}

int key_remove(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device removed\n");
    if (global_irq >= 0) {
        free_irq(global_irq, pdev);
        global_irq = -1;
    }
    if (global_key_gpio >= 0) {
        gpio_free(global_key_gpio);
        global_key_gpio = -1;
    }
    misc_deregister(&key_misc_device);
    return 0;
}

struct platform_driver key_platform_driver = {
    .probe = key_probe,
    .remove = key_remove,
    .driver = {
        .owner = THIS_MODULE,
        .name = "key",
        .of_match_table = (const struct of_device_id []) {
            {.compatible = "test,key"},
            {}
        },
    }
};


// 驱动入口 *************************************************************
static int __init wait_queue_init(void)
{
    printk(KERN_INFO "Wait queue module init\n");
    return 0;
}

static void __exit wait_queue_exit(void)
{
    printk(KERN_INFO "Wait queue module exit\n");
}

module_init(wait_queue_init);
module_exit(wait_queue_exit);
```

