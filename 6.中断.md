### 中断系统

#### 中断基础

CPU 在正常运行期间，由于外部或内部引起的事件，让 CPU 停下当前正在运行的程序，转而去执行触发中断所对应的程序

1. 中断请求
2. 中断响应
3. 保护现场
4. 中断处理
5. 恢复现场
6. 中断返回
7. 中断屏蔽

##### 中断上下文

中断可以提高 CPU 的运行效率，但是中断会打断内核进程中正常调度和运行，为保证系统实时性，中断服务必须足够简短

但是实际应用中可能存在某些是否发生中断事必须处理大量事务，这时候如果都在中断服务中完成，会严重降低中断的实时性

因此 Linux 提出了，把中断分为两部分：把中断分为`中断上文`和`中断下文`

- 中断上文：完成进可能少切比较急的任务，中断上文的特点就是响应速度快
- 中断下文：处理中断剩余的大量比较耗时间的任务，而且可以被新的中断打断
- 总结：中断上文越快越好，中断下文做比较耗时间的问题，但是不能死循环

##### Linux 中断可以嵌套吗

不可以

#### 中断相关应用

##### 设备树中的中断节点

`RK356x`对其中断控制器的定义

```C 
gic: interrupt-controller@fd400000 {
    // 在系统中，这个节点由 ARM GICv3 驱动来处理
	compatible = "arm,gic-v3";
    // 定义了 GIC 被其他设备作为中断控制器引用时，一个中断描述需要 3 个 cell
	#interrupt-cells = <3>;
    // reg 地址有几个
	#address-cells = <2>;
	#size-cells = <2>;
    // 用于父子节点之间的地址空间映射；空 ranges 表示不做地址转换
	ranges;
    // 这个节点本身是一个 Interrupt Controller，可以被其他设备作为中断控制器引用。
	interrupt-controller;

    // GIC 的 MMIO 寄存器区域
	reg = <0x0 0xfd400000 0 0x10000>, /* GICD */
		  <0x0 0xfd460000 0 0xc0000>; /* GICR */
    // GIC 自己也需要通过一个中断通知 CPU，这个中断属于 PPI 9，并且高电平触发
	interrupts = <GIC_PPI 9 IRQ_TYPE_LEVEL_HIGH>;
    // PCIE 设备使用的中断节点
	its: interrupt-controller@fd440000 {
		compatible = "arm,gic-v3-its";
		msi-controller;
		#msi-cells = <1>;
		reg = <0x0 0xfd440000 0x0 0x20000>;
	};
};
```

然后，`rk356x`在其设备树的根节点定义了

```C
/ {
	interrupt-parent = <&gic>;
	#address-cells = <2>;
	#size-cells = <2>;
    //...
};
```

这表示根节点下的所有节点，如果没有指定`interrupt-parent`，那么它的`interrupts`属性默认就有这个`&gic`中断控制器解释

对于一个`GPIO`节点

```C
gpio3: gpio@fe760000 {
	compatible = "rockchip,gpio-bank";
	reg = <0x0 0xfe760000 0x0 0x100>;
	interrupts = <GIC_SPI 36 IRQ_TYPE_LEVEL_HIGH>;
	clocks = <&cru PCLK_GPIO3>, <&cru DBCLK_GPIO3>;
    gpio-controller;
	#gpio-cells = <2>;
	interrupt-controller;
	#interrupt-cells = <2>;
};
```

这个 `GPIO控制器`也可以作为一个 中断控制器，但他没有定义自己的`interrupt-parent`，那么他会想其父节点寻找，知道继承根节点的`interrupt-parent = <&gic>;`中断控制器

那就又引出一个问题：`gic`自身的中断向谁传递，从设备树代码来看，`gic`定义了自己的`interrupts`，理应存在它的`interrupt-parent`，但是它自己并没有定义，于是向父节点一路寻找，最后在根节点找到了`interrupt-parent = <&gic>;`

这就发现`gic`全局中断控制器的的中断控制器指向了自己，在语义上是把自己接收到的中断打包推向自己，这是什么情况呢？

根源就是`compatible = "arm,gic-v3";`的驱动定义，这条`compatible`让 Linux 内核使用 ARM 是`gic-v3`驱动处理这个 `gic`

在 `drivers/irqchip/irq-gic-v3.c`中有定义`IRQCHIP_DECLARE(gic_v3, "arm,gic-v3", gic_of_init);`，表明了使用`gic_of_init`函数来初始化`arm,gic-v3`这个中断控制器

在`gic_of_init`中，负责映射`gic`的`reg`，设置`GIC`硬件的中断使能/优先级，设置 Linux 的 `irq_chip`并建立`irq_domain`，注册到 Linux 的 IRQ 子系统

这里着重了解一下 `irq_domain`，还是从 `GPIO`引脚产生的中断看起

在系统开机时，`GIC`在`gic_of_init`中，通过驱动向内核申明了自己能够管理外部中断，并在 Linux 的 `irq_domain`中建立自己的`IRQ Number`

`GPIO3`收到外部的电气信号，产生 `SPI 36` 的中断，并将中断提交给 `GIC`，`GIC`将这个信号转发给`CPU`，从而触发 Linux 内核中的`irq handler`，在`irq handler`中读取由那个硬件触发的中断，解析出`SPI 36`然后通过`irq_domain`转换为`Linux IRQ Number`，然后通过内核映射，进入注册到的`GPIO3`中断程序

---

回到应用层面，我们如何将一个按钮写入设备树呢：

```C
key {
    compatible = "key";
    
    pinctrl-name = "default";
    pinctrl-0 = <&pinctrl_key>;
    // 还是 GPIO3_A5，低电平触发
    key-gpios = <&gpio3 RK_PA5 GPIO_ACTIVE_LOW>;
    
    interrupt-parent = <&gpio3>;
    interrupts = <RK_PA5 IRQ_TYPE_LEVEL_LOW>;
    
    status = "okay";
}

&pinctrl {
	pinctrl_key:key {
		// RK 的引脚定义是 <Bank Pin Mux Config>
		rockchip,pins = <
			RK_GPIO3 RK_PA5 RK_FUNC_0 &pcfg_pull_none_smt
		>;
	};
};
```

#### 中断相关函数

##### 1. 获取中断号相关函数

编写驱动时需要用到中断号，每一个中断都有中断号，这个中断号是在设备树中定义的，可以通过 `irq_of_parse_and_map`函数从`interrupts`属性中提取出相关的中断号

```C
// dev: 设备节点
// index: interrpuits 属性肯呢个包含多条中断信息，通过 index 索引指定 
unsigned int irq_number = irq_of_parse_and_map(struct device_node *dev, int index);
// 返回中断号
```

如果是使用`GPIO`中断的话，可以使用`gpio_to_irq`来或区域`GPIO`对应的中断号

```C
// 要获取的 GPIO 编号
int gpio_irq_number = gpio_to_irq(unsigned int gpio);
// GPIO 对应的中断号
```

##### 2. 申请中断函数

和 `GPIO` 一样，使用某个中断也是需要申请的

```C
// irq: 要申请中断的中断号
// handler: 中断处理函数
// flags: 中断标志，在 include/linux/interrupt.h 
// name: 中断名字
// dev: 当 flags 为 IRQF_SHARED 时，dev 用于区分不同的中断 
int ret = request_irq(unsigned int irq,
                      irq_handler_t handler,
                      unsigned long flags,
                      const char *name,
                      void *dev);

```

| flags 标志           | 功能                                                         |
| -------------------- | ------------------------------------------------------------ |
| IRQF_SHARED          | 多个设备共享一个中断，共享的所有中断都必须指定该标识。如果使用共享中断，啧必须使用 dev 参数来区分 |
| IRQF_ONESHOT         | 单次中断，执行一次就结束                                     |
| IRQF_TRIGGER_NONE    | 无触发                                                       |
| IRQF_TRIGGER_RISING  | 上升沿触发                                                   |
| IRQF_TRIGGER_FALLING | 下降沿触发                                                   |
| IRQF_TRIGGER_HIGH    | 高电平触发                                                   |
| IRQF_TRIGGER_LOW     | 低电平触发                                                   |

##### 3. 中断处理函数

使用 `request_irq`函数是申请中断的时候需要设置中断处理函数

```C
// 第一个参数是中断处理程序所对应的中断号
// 第二个参数是一个通用指针，需要和 request_irq 中的 dev 类型一致
irqreturn_t (*irq_handler_t) (int, void *);
// 返回值是 irqreturn_t 枚举
enum irqreturn {
    IRQ_NONE = (0 << 0),		// 不处理
    IRQ)HANDLED = (1 << 0),		// 正常处理
    IRQ_WAKE_THREAD = (1 << 1),	// 转到中断下文处理
};
typedef enum irqreturn irqreturn_t;
// 可以通过以下方式返回
return IRQ_RETVAL(IRQ_HANDLED);
```

##### 4. 中断释放函数

中断使用完成以后就要通过 `free_irq` 函数释放对应中断。

```C
// irq: 要释放的中断号
// dev: 如果中断设置为共享(IRQF_SHARED)，此参数用于区分具体的中断。
void free_irq(unsigned int irq, void *dev);
```

#### 按键中断

##### 设备树部分

对按键编写设备树

```C
test_key {
    compatible = "test,key";
    
    pinctrl-names = "default";
    pinctrl-0 = <&pinctrl_test_key>;
    key-gpios = <&gpio3 RK_PA5 GPIO_ACTIVE_LOW>;
    
    // 如果不使用 gpio_to_irq 而是使用 irq_of_parse_and_map，则需要使用以下配置
    interrupt-parent = <&gpio3>;
    interrupts = <RK_PA5 IRQ_TYPE_EDGE_BOTH>;
    
    status = "okay";
}

&pinctrl {
    pinctrl_test_key:key {
		// RK 的引脚定义是 <Bank Pin Mux Config>
		rockchip,pins = <
			RK_GPIO3 RK_PA5 RK_FUNC_0 &pcfg_pull_none_smt
		>;
	};
}
```

##### C代码部分

针对该设备树编写驱动c代码

```C
#include <linux/module.h>

#include <linux/platform_device.h>
#include <linux/of.h>
#include <linux/gpio.h>
#include <linux/of_gpio.h>

#include <linux/interrupt.h>
#include <linux/of_irq.h>

MODULE_LICENSE("GPL");

static int global_key_gpio = -1;
static int global_irq = -1;

// 中断处理函数
irqreturn_t key_irq_handler(int irq, void *dev_id)
{
    printk(KERN_INFO "Key IRQ triggered\n");
    return IRQ_HANDLED;
}

int key_probe(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device probed\n");

    int ret = 0;

    struct device_node *key_device_node = pdev->dev.of_node;

    // 申请 gpio 资源
    int key_gpio = of_get_named_gpio(key_device_node, "key-gpios", 0);
    if (key_gpio < 0) {
        printk(KERN_ERR "Failed to get key-gpio\n");
        return key_gpio;
    }

    global_key_gpio = key_gpio;

    // 设置 gpio 为输入
    ret = gpio_request(key_gpio, "key_gpio");
    if (ret) {
        printk(KERN_ERR "Failed to request key-gpio\n");
        goto func_error;
    }
    ret = gpio_direction_input(key_gpio);
    if (ret) {
        printk(KERN_ERR "Failed to set key-gpio direction\n");
        goto func_error;
    }

    // 将 GPIO 口设置为中断
    int irq = gpio_to_irq(key_gpio);
    if (irq < 0) {
        printk(KERN_ERR "Failed to get key-gpio irq\n");
        ret = irq;
        goto func_error;
    }

    // 也可以使用通用方法
    int common_irq = irq_of_parse_and_map(key_device_node, 0);
    if (common_irq < 0) {
        printk(KERN_ERR "Failed to parse and map irq\n");
        ret = common_irq;
        goto func_error;
    }
    printk(KERN_INFO "Key GPIO: %d, IRQ: %d, Common IRQ: %d\n", key_gpio, irq, common_irq);
    // 两者在理论上应该是相同的

    // 请求中断
    // 设置上升沿和下降沿触发中断
    ret = request_irq(irq, key_irq_handler, IRQF_TRIGGER_FALLING | IRQF_TRIGGER_RISING, "key_irq", pdev);
    if (ret) {
        printk(KERN_ERR "Failed to request key-gpio irq\n");
        goto func_error;
    }
    global_irq = irq;

    return 0;

func_error:
    if (global_key_gpio >= 0) {
        gpio_free(global_key_gpio);
    }
    if (global_irq >= 0) {
        free_irq(global_irq, pdev);
    }

    return ret;
}

int key_remove(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device removed\n");

    if (global_irq >= 0) {
        free_irq(global_irq, pdev);
    }
    if (global_key_gpio >= 0) {
        gpio_free(global_key_gpio);
    }

    return 0;
}

struct platform_driver key_platform_driver = {
    .probe = key_probe,
    .remove = key_remove,
    .driver = {
        .owner = THIS_MODULE,
        .name = "key",
        .of_match_table = (const struct of_device_id []) {
            {.compatible = "test,key"},
            {}
        },
    }
};


static int __init _key_init(void)
{
    printk(KERN_INFO "Key driver init\n");

    return platform_driver_register(&key_platform_driver);
}

static void __exit key_exit(void)
{
    platform_driver_unregister(&key_platform_driver);
}

module_init(_key_init);
module_exit(key_exit);

MODULE_LICENSE("GPL");
```

---



#### 中断下文之`tasklet`

##### `tasklet`基本定义

`tasklet`是中断处理中断下文 常用的一种方法，`tasklet`是一种特殊的软中断。处理中断下文的机制还有工作队列和软中断。

###### 如何使用`tasklet`

Linux 把中断分成上文和下文两个部分，在中断上文只处理比较紧急的事情，同时可以调用`tasklet`来启动中断下文，比较耗时间的事情放到中断下文来处理。

调用`tasklet`后，`tasklet`绑定的函数并不会马上执行，而是在中断退出后，经过一个很短但不确定的时间之后来执行。

###### `tasklet`定义

`tasklet`由`tasklet_struct`结构表示，每个结构体表示一个`tasklet`，具体定义为：

```C
struct tasklet_struct
{
    // 链表中的下一个 tasklet
	struct tasklet_struct *next;
	unsigned long state;
	atomic_t count;
	bool use_callback;
    // tasklet 绑定的参数
	union {
		void (*func)(unsigned long data);
		void (*callback)(struct tasklet_struct *t);
	};
    // 传递给函数的参数
	unsigned long data;
};
```

##### `tasklet`相关函数

###### 1. `tasklet_schedule` 函数

用于调度`tasklet`

```C
//t: 指向 tasklet 的指针
void tasklet_schedule(struct tasklet *t);
```

###### 2. `tasklet_init`函数

动态初始化`tasklet`

```C
// t: 指向 tasklet 的指针
// func: tasklet 绑定的函数
// data: 函数执行时要传递的参数
void tasklet_init(struct tasklet_struct *t, void(*func)(unsigned long), unsigned long data);
```

###### 3. `tasklet_kill`函数

删除一个`tasklet`

```C
void tasklet_kill(struct tasklet_struct *);
```

###### 使用 `tasklet`的步骤

1. 定义一个 `tasklet` 结构体
2. 动态初始化 `tasklet`
3. 编写 `tasklet` 绑定的函数
4. 在中断上文调用 `tasklet`
5. 卸载模块时删除`tasklet
6. 

```C
#include <linux/module.h>

#include <linux/platform_device.h>
#include <linux/of.h>
#include <linux/of_gpio.h>

#include <linux/interrupt.h>

#include <linux/delay.h>


static int global_key_gpio = -1;
static int global_irq = -1;

struct tasklet_struct key_tasklet;

irqreturn_t key_irq_handler(int irq, void *dev_id)
{
    printk(KERN_INFO "Key IRQ triggered\n");

    // 触发 tasklet
    tasklet_schedule(&key_tasklet);

    printk(KERN_INFO "Key IRQ handled\n");

    return IRQ_HANDLED;
}

// tasklet 处理函数
void key_tasklet_handler(unsigned long data)
{
    struct platform_device *pdev = (struct platform_device *)data;
    printk(KERN_INFO "Tasklet executed for device: %s\n", dev_name(&pdev->dev));
    ssleep(1); // 模拟耗时操作
    // 事实上，可以是写文件、发送信号等操作
    printk(KERN_INFO "Tasklet completed for device: %s\n", dev_name(&pdev->dev));
}


int key_probe(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device probed\n");

    struct device_node *node = pdev->dev.of_node;
    if (!node) {
        printk(KERN_ERR "Failed to get device node\n");
        return -ENODEV;
    }

    // 获取 GPIO 资源
    int key_gpio = of_get_named_gpio(node, "key-gpios", 0);
    if (key_gpio < 0) {
        printk(KERN_ERR "Failed to get key-gpio\n");
        return key_gpio;
    }
    int ret = gpio_request(key_gpio, "key_gpio");
    if (ret) {
        printk(KERN_ERR "Failed to request key-gpio\n");
        return ret;
    }
    ret = gpio_direction_input(key_gpio);
    if (ret) {
        printk(KERN_ERR "Failed to set key-gpio direction\n");
        gpio_free(key_gpio);
        return ret;
    }

    global_key_gpio = key_gpio;

    gpio_direction_input(key_gpio);
    gpio_set_debounce(key_gpio, 200); // 设置消抖时间为 200ms

    int irq = gpio_to_irq(key_gpio);
    if (irq < 0) {
        printk(KERN_ERR "Failed to get key-gpio irq\n");
        gpio_free(key_gpio);
        return irq;
    }
    ret = request_irq(irq, key_irq_handler, IRQF_TRIGGER_FALLING | IRQF_TRIGGER_RISING, "key_irq", pdev);
    if (ret) {
        printk(KERN_ERR "Failed to request key-gpio irq\n");
        gpio_free(key_gpio);
        return ret;
    }

    global_irq = irq;

    // 初始化 tasklet
    tasklet_init(&key_tasklet, key_tasklet_handler, (unsigned long)pdev);

    return 0;
}

int key_remove(struct platform_device *pdev)
{
    printk(KERN_INFO "Key device removed\n");
    if (global_irq >= 0) {
        free_irq(global_irq, pdev);
        global_irq = -1;
    }
    if (global_key_gpio >= 0) {
        gpio_free(global_key_gpio);
        global_key_gpio = -1;
    }
    return 0;
}

struct platform_driver tasklet_platform_driver = {
    .probe = key_probe,
    .remove = key_remove,
    .driver = {
        .owner = THIS_MODULE,
        .name = "key",
        .of_match_table = (const struct of_device_id []) {
            {.compatible = "test,key"},
            {}
        },
    }
};

static int __init _tasklet_init(void)
{
    printk(KERN_INFO "Tasklet module init\n");
    return platform_driver_register(&tasklet_platform_driver);
}

static void __exit tasklet_exit(void)
{
    printk(KERN_INFO "Tasklet module exit\n");
    platform_driver_unregister(&tasklet_platform_driver);
    tasklet_kill(&key_tasklet); // 确保 tasklet 已经完成
}

module_init(_tasklet_init);
module_exit(tasklet_exit);

MODULE_LICENSE("GPL");
```









