### 并发与竞态

当多个执行单元（进程、线程、中断）并发地访问同一共享资源时，便会发生竞争。

竞争会共享资源处于不一致的状态，进而引发一系列问题，因此 Linux 引入了：**原子操作、自旋锁、信号量、互斥锁** 等同步机制，以确保对共享资源的访问是串行且安全的

#### 原子操作

原子操作是在执行过程中不可被中断的操作，一个原子操作要么完整的被执行，要么完全不执行

##### 代码定义

内核提供了 `atomic_t` 和 `atomic64_t` 两个结构体分别定义 32位 和 64位 环境中的原子变量

```C
static atomic64_t value;
```

##### 原子操作接口函数

1. `ATOMIC_INIT` 宏

   定义原子变量并初始化

   ```C
   // i: 要初始化的整数
   static atomic64_t value = ATOMIC_INIT(int i);
   ```

2. `atomic_read`函数

   读取原子变量的值

   ```C
   int v = atomic_read(value);
   ```

3. `atomic_set`函数

   ```C
   atmoic_set(value, 2);
   ```

4. `atomic_add(10, value)`

5. `atomic_dec(20, value)`

6. `atomic_inc(value)`

7. `atomic_dev(value)`

8. ...

##### 位原子操作

位原子操作直接作用于指定的内存地址及其特定位

1. `set_bit` 函数

   将一个特定地址的特定位置为 1

   ```C
   // nr: 第 nr 位
   // p:  地址指针
   void set_bit(int nr, void *p);
   ```

2. `clear_bit`函数

   将一个特定地址的特定位置为 0

3. `change_bit`函数

   翻转

4. `test_bit`函数

   用于获取一个特定位的值

   ```C
   int b = test_bit(int nr, void *p);
   ```

##### 示例代码

通过原子操作保护一个二员状态标志，保护设备的互斥打开（即同一时间只能有一个进程打开设备）

```C
#include <linux/atomic.h>
// 设置原子变量表示设备可用状态，0表示不可用（有占用），否则表示可用
static atomic64_t v;
static int open_misc(struct inode *inode, struct file *file) {
    if (atomic64_read(&v) != 1) {
        // 当设备可用
        return -EBUSY;
    }
    // 如果到这，说明设备没有占用
    // 则将设备设置为不可用
    atomic64_set(&v, 0);
    do_something();
	return 0;
}
static int release_misc(struct inode *inode, struct file *file) {
    // 在设备释放前，将状态可是为可用，以给其他设备获取的机会
    atomic64_set(&v, 1);
    return 0;
}
```

#### 自旋锁

自旋锁是一种忙等待锁，当一个执行单元尝试获取已被占用的自选锁时，进程会进入一个紧凑循环以持续检查锁的状态，直到成功获取锁为止

##### 代码定义

```C
static spinlock_t lock; 
```

##### 自旋锁相关接口

1. `DEFINE_SPINLOCK` 宏

   定义并初始化自旋锁

   ```C
   static DEFINE_SPINLOCK(lock);
   ```

2. `spin_lock_init`函数

   初始化自旋锁

   ```C
   int spin_lock_init(spinlock_t *lock);
   ```

3. `spin_lock`函数

   获取指定的自旋锁

   ```C
   void spin_lock(spinlock_t *lock);
   ```

   如果获取成功则立刻返回，否则持续阻塞，直到获取锁

4. `spin_unlock`函数

   释放指定的自旋锁

   ```C
   void spin_unlock(spinlock_t *lock);
   ```

5. `spin_trylock`函数

   尝试获取指定的自旋锁，如果获取成功则返回1，否则返回0

   这个函数不会产生阻塞，由程序自行判断有无获取到锁之后的操作

   ```C
   int spin_trylock(spinlock_t *lock);
   ```

6. `spin_is_locked`函数、

   检查锁是否被其他进程占用中

   ```C
   int spin_is_locked(spinlock_t *lock);
   // 没有被占用返回1，否则返回0
   ```

7. `spin_lock_irq`函数

   在加锁的同时禁止本地中断

   为了防止在主进程和硬件中断同时访问同一块内存导致如下情况发生：

   - 主进程加锁，修改数据，在释放锁之前（甚至在修改数据过程中）发生中断，在该中断中又要对数据进行修改，于是尝试获取锁，然后发现主进程没释放锁，于是导致死锁。
   
     所以我们可以在加锁时禁用中断，以防止死锁
   
   ```C
   void spin_lock_irq(spinlock_t *lock);
   ```
   
8. `spin_unlock_irq`函数

   在释放锁的同时允许本地中断

9. `spin_lock_irqsave`函数

   `spin_lock_irq`会引入问题，就是我本来就关着中断，然后释放锁的时候打开了，那不是完蛋了

   所以我们用 `spin_lock_irqsave`函数，保存当前中断情况，在后续恢复

10. `spin_lock_irqrestore`函数

   恢复 `spin_lock_irqsave`保存的中断状态

11. ...

##### 示例代码

```C
#include <linux/spinlock.h>
static DEFINE_SPINLOCK(lo);

// 1 表示设备没有被占用，0 表示设备正在被使用
static int flag = 1;

static int misc_open(struct inode *inode, struct file *file) {
    // 我们使用自旋锁来保护 flag，必须持有锁才能得到 flag 的修改权
    spin_lock(&lo);
   	if (flag != 1) {
        // 我们没有得到设备使用权
        // 记得放弃锁让别的进程能查看或修改 lo
        spin_unlock(&lo);
        return -EBUSY;
    }
    // 我们发现设备及时空闲的，于是可以占用
    flag = 0;
    spin_unlock(flag);
    do_something();
    return 0;
}

static int misc_release(struct inode *inode, struct file *file) {
    do_something_to_release();
    spin_lock(&lo);
    // 设备还给你
    flag = 1;
    spin_unlock(&lo);
    return 0;
}

static int __init mo_init() {
    spin_lock_init(&lo);
    register_misc(xxx)
}
```

#### 信号量

信号量是一种允许线程在锁被占用时进入睡眠状态的同步机制，与自旋锁的忙等待不同，当一个任务尝试获取已被占用的锁时，他会被放入等待队列并让出处理器。待信号量持有者的锁释放后，等待队列中的任务会被唤醒并尝试重新获取信号量。这种信号量非常适合锁持有时间长（如阻塞的输入输出操作），从而避免忙等待造成的CPU消耗

##### 代码定义

```C
static struct semaphore;
```

##### 信号量接口函数

1. `DEFINE_SEMAPHORE`宏

   定义并初始化信号量

   ```C
   static DEFINE_SEMAPHORE(sem);
   ```

2. `sema_init`函数

   初始化信号量，并设置初始值

   ```C
   // sem: 信号量的指针
   // val: 要初始化的值
   void sema_init(struct semaphore *sem, int val);
   ```

3. `down`函数

   尝试获得信号量，当信号量不可用时，进程进入不可中断的睡眠状态

   ```C
   void down(struct semaphore *sem);
   ```

4. `down_interruptible`函数

   尝试获取信号量，但这次这个允许进程被中断

   ```C
   int down_interruptible(struct *semaphore sem);
   // 如果正常获取到锁，则返回 0
   // 如果在等待获取锁期间发生中断，则返回 -EINTR
   ```

5. `up`函数

   释放信号量

   ```C
   void up(struct semaphore *sem);
   ```

6. `down_trylock`函数

   尝试获取信号量，如果能获取到则返回 0，反之则返回非0

   ```C
   int down_trylock(struct semaphore *sem);
   ```

##### 示例代码

```C
#include <linux/semaphore.h>
static DEFINE_SEMAPHORE(se);

static int misc_open(struct inode *inode, struct file *file) {
    down(se);
    do_something();
    return 0;
}

static int misc_release()(struct inode *inode, struct file *file) {
    do_something_for_release();
    up(se);
    return 0;
}

static int __init mo_init() {
    // 设置这个信号量有 2 个可用
    sema_init(se, 2);
}
```

在该例程中，该设备可以同时被两个进程访问

#### 互斥锁

互斥锁是 Linux 内核中用于试下女互斥访问的同步机制，功能上类似于初始值为1的二元信号量，但性能极优，是保护临界区的首选方案

##### 代码定义

```C\
static struct mutex mu;
```

##### 互斥锁接口函数

1. `DEFINE_MUTEX`宏

   定义并初始化互斥锁

   ```C
   static DEFINE_MUTEX(mu);
   ```

2. `mutex_init`函数

   初始化

3. `mutex_lock`

4. `mutex_unlock`

5. `mutex_is_locked`

##### 示例代码

```C
#include <linux/mutex.h>
static DEFINE_MUTEX(mu);
mutex_init(mu);
mutex_lock(mu);
// 临界区
mutex_unlock(mu);
```



   

