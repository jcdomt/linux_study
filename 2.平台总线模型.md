### 平台总线模型

platform 总线模型。是 Linux 内核虚拟出来的一条总线。把原来的驱动c文件拆成了两个c文件，一个是 `device.c`一个`driver.c`

把稳定的逻辑放在 `driver.c` ，需要改变的/和硬件相关的写在 `device.c`



#### 注册一个 device

`device.c` 里面写的是硬件资源，这里的硬件资源是寄存器的地址、中断号、时钟等硬件资源

```c
struct platform_device {
    const char *name;	// 平台总线进行匹配时用到的name，/sys/bus/...
    int id;				// 设备 id，一般为 -1
    struct device dev;	// 内嵌的 device
    u32 num_resources;	// 资源数量
    struct resource *resource; // 硬件资源  
}

struct resource {
    resource_size_t start;
    resource_size_t end;
    const char *name;
    unsigned long flags;
}

#define IOREOSOURCE_IO		IO的内存
#define IOREOSOURCE_MEM		表示一段物理内存
#define IOREOSOURCE_IRQ		表示中断号
 
```

#### 注册一个 driver

实现 `platform_driver`，当 `driver`和 `device`匹配时，就会执行 `probe`函数

```c
struct platform_driver {
    // 匹配
	int (*probe)(struct platform_device *);

    // device 和 driver 任意一个被删除
	int (*remove)(struct platform_device *);

	void (*shutdown)(struct platform_device *);
	int (*suspend)(struct platform_device *, pm_message_t state);
	int (*resume)(struct platform_device *);
    
    // 匹配 driver.name 的设备名
	struct device_driver driver;
    // 优先匹配 id_table.name 的设备名
	const struct platform_device_id *id_table;
};

struct device_driver {
    // 匹配时用到的名字
    const char *name;
    struct module *owner;
    
}
```

#### probe 函数编写

1. 从 `device.c` 中获取到硬件资源

   ```c
   // 方法一：直接获取（不推荐）
   static int beep_probe(struct platform_device *pdev) {
       // pdev 就是 device.c 注册的 platform_device
       char *name = pdev->resource[0].name;
   }
   
   // 方法二：函数获取
   static int beep_probe(struct platform_device *pdev) {
       // 获取 pdev 中 IOREOSOURCE_MEM 类型资源的第 0 哥
   	struct resource *beep_mem = platform_get_resource(pdev, IOREOSOURCE_MEM, 0);
       if (beep_mem == NULL) {
           return -EBUSY;
       }
       resource_size_t start = beep_mem.start;
   }
   ```

   

2. 注册 杂项/字符 设备

   注册之前先登记

   ```c
   // start: 起始地址
   // n: 请求地址长度
   // name
   request_mem_region(start, n, name) {}
   
   // 通知内核占用本内存区域
   struct resource *res = request_mem_region(pdev->resource[0].start, pdev->resource[0].end-pdev->resource[0].start+1, "name");
   
   ```

   

   

