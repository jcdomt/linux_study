### i2c 通信

#### 基础知识

**i2c** 是一种总线协议，使用两条线实现主控制器和从机直接的数据传输：一条是 **SCL** 串行时钟线，一条是 **SDA** 串行数据线；总线空闲时这两条线都处于高电平

在单片机上，开发者需要自己模拟 **i2c** 时序，但是在 **Linux** 系统中，**CPU** 自带 **I2C** 控制器，我们只需要关心怎么把数据写到寄存器和从寄存器读取数据即可，具体的时序由控制器帮我们自动完成

**Linux** 把 **I2C控制器** 抽象成`i2c_adapter`，我们只需要来分配这个 `i2c_adapter` 就可以获得一个控制器

#### 在应用层操作 i2c

##### 结构体定义

用户应用可以通过读取 `/dev/i2c-xxx` 的节点来读写总写数据

数据包对应的结构体是定义在 `include/uapi/linux/i2c-dev.h`的`i2c_rdwr_ioctl_data`中

```c
struct i2c_msg {
	__u16 addr;
	__u16 flags;
#define I2C_M_RD		0x0001	/* guaranteed to be 0x0001! */
#define I2C_M_TEN		0x0010	/* use only if I2C_FUNC_10BIT_ADDR */
#define I2C_M_DMA_SAFE		0x0200	/* use only in kernel space */
#define I2C_M_RECV_LEN		0x0400	/* use only if I2C_FUNC_SMBUS_READ_BLOCK_DATA */
#define I2C_M_NO_RD_ACK		0x0800	/* use only if I2C_FUNC_PROTOCOL_MANGLING */
#define I2C_M_IGNORE_NAK	0x1000	/* use only if I2C_FUNC_PROTOCOL_MANGLING */
#define I2C_M_REV_DIR_ADDR	0x2000	/* use only if I2C_FUNC_PROTOCOL_MANGLING */
#define I2C_M_NOSTART		0x4000	/* use only if I2C_FUNC_NOSTART */
#define I2C_M_STOP		0x8000	/* use only if I2C_FUNC_PROTOCOL_MANGLING */
	__u16 len;
	__u8 *buf;
};

struct i2c_rdwr_ioctl_data {
	struct i2c_msg *msgs;	/* pointers to i2c_msgs */
	__u32 nmsgs;			/* number of i2c_msgs */
};
```

`i2c_rdwr_ioctl_data` 中的 `nmsgs` 为预发送的数据包数量

`i2c_msg` 结构体中，`addr` 为从机地址，`flags`为 `1` 则为读，为 `0` 则为写

##### 读取使用的接口

可以使用 `ioctl` 命令读写 `i2c` 的设备节点

1. `I2C_RDWR` 命令

   该命令用于统一读写

##### 应用层完整代码

```C
#include <fcntl.h>

#include <linux/i2c-dev.h>
#include <linux/i2c.h>

#include <stdio.h>
#include <unistd.h>
#include <sys/ioctl.h>

int fd;

int i2c_read_data(unsigned int slave_addr, unsigned char reg_addr) {
    // 用来存储读取到的数据
    unsigned char data;

    struct i2c_rdwr_ioctl_data i2c_read_led;
    struct i2c_msg msgs[2] = {
        {
            // 从机地址
            .addr = slave_addr,
            // 写
            .flags = 0,
            // 写入寄存器地址，表示下一步要读取的寄存器
            .len = sizeof(reg_addr),
            .buf = &reg_addr
        },
        {
            .addr = slave_addr,
            // 读
            .flags = I2C_M_RD,
            .len = sizeof(data),
            .buf = &data
        }
    };

    i2c_read_led.msgs = msgs;
    i2c_read_led.nmsgs = sizeof(msgs) / sizeof(msgs[0]);

    // 执行I2C_RDWR ioctl命令
    if (ioctl(fd, I2C_RDWR, &i2c_read_led)) {
        perror("Failed to read from i2c device");
        return -1;
    }

    return data;
}

int main() {
    fd = open("/dev/i2c-1", O_RDWR);
    if (fd < 0) {
        perror("Failed to open i2c device");
        return 1;
    }

    
    while (1) {
        // 根据硬件资源获取地址
        int data = i2c_read_data(0x38, 0x20);
        if (data < 0) {
            return 1;
        }

        printf("Data read from I2C device: 0x%02X\n", data);
        sleep(1);
    }

    return 0;
}
```

### i2c 驱动

Linux 中的 `i2c` 是根据平台总线模型设计的

#### 非设备树版本

在非设备树的模式下，我们使用 `i2c_board_info` 结构体来描述一个 i2c设备，定义如下

```C
struct i2c_board_info {
	char		type[I2C_NAME_SIZE];
	unsigned short	flags;
	unsigned short	addr;
	const char	*dev_name;
	void		*platform_data;
	struct device_node *of_node;
	struct fwnode_handle *fwnode;
	const struct software_node *swnode;
	const struct resource *resources;
	unsigned int	num_resources;
	int		irq;
};
```

其中的 `type` 和 `addr` 是必须要设置的：一个是从机设备的名字，用于驱动匹配；一个是从机设备的地址

##### 非设备树驱动接口

1. `i2c_get_adapter` 函数

   获取一个 i2c 适配器

   ```C
   // nr: 预获取的适配器编号
   struct i2c_adapter *i2c_get_adapter(int nr);
   // 失败返回 NULL
   ```

2. `i2c_put_adapter` 函数

   释放 `i2c` 适配器

   ```C
   void i2c_put_adapter(struct i2c_adapter *adap);
   ```

3. `i2c_new_client_device`函数

   将 i2c适配器 和 i2c设备 关联起来

   ```C
   // adap: i2c 适配器
   // info: 在 i2c_board_info 中定义的 i2c client
   struct i2c_client *i2c_new_client_device(struct i2c_adapter *adap, struct i2c_board_indo const *info);
   ```

4. `i2c_unregister_device` 函数

   注销一个 client

   ```C
   void i2c_unregister_device(i2c_client *cli);
   ```

##### 非设备树设备驱动

```C
#include <linux/module.h>
MODULE_LICENSE("GPL");

#include <linux/i2c.h>
#include <linux/i2c-dev.h>

struct i2c_board_info i2c_board_info[] = {
    {
        .type = "i2c_device",
        .addr = 0x38,
    },
    {}
};

static struct i2c_adapter *i2c_adapter;
static struct i2c_client *i2c_client;

static int __init i2c_device_init(void) {
    printk(KERN_INFO "I2C device module initialized\n");

    i2c_adapter = i2c_get_adapter(0);
    if (!i2c_adapter) {
        printk(KERN_ERR "Failed to get I2C adapter\n");
        return -ENODEV;
    }
    i2c_client = i2c_new_client_device(i2c_adapter, &i2c_board_info[0]);
    if (!i2c_client) {
        printk(KERN_ERR "Failed to create I2C client\n");
        i2c_put_adapter(i2c_adapter);
        return -ENODEV;
    }

    return 0;
}

static void __exit i2c_device_exit(void) {
    printk(KERN_INFO "I2C device module exited\n");

    i2c_unregister_device(i2c_client);
    i2c_put_adapter(i2c_adapter);
}

module_init(i2c_device_init);
module_exit(i2c_device_exit);
```

#### 使用设备树

当然，其实上面这些很复杂的函数都没啥用，因为我们可以用设备树

我们只需要在对应设备树的节点下创建相应设备的节点即可，我们拿开发板厂家的设备树为例

```C
i2c0: i2c@fdd40000 {
	compatible = "rockchip,rk3568-i2c", "rockchip,rk3399-i2c";
	reg = <0x0 0xfdd40000 0x0 0x1000>;
	interrupts = <GIC_SPI 46 IRQ_TYPE_LEVEL_HIGH>;
	clocks = <&pmucru CLK_I2C0>, <&pmucru PCLK_I2C0>;
	clock-names = "i2c", "pclk";
	pinctrl-0 = <&i2c0_xfer>;
	pinctrl-names = "default";
	#address-cells = <1>;
	#size-cells = <0>;
	status = "disabled";
};


&i2c0 {
	status = "okay";
	rk809: pmic@20 {
		rk809_codec: codec {
			pinctrl-names = "default", "spk_gpio";
			pinctrl-0 = <&i2s1m0_mclk>;
			pinctrl-1 = <&spk_ctl_gpio>;
			spk-ctl-gpios = <&gpio1 RK_PB1 GPIO_ACTIVE_HIGH>;
			status = "okay";
		};

		rk809_rtc:rtc{
			status = "disabled";
		};
	};

	hym8563: hym8563@51 {
		compatible = "haoyu,hym8563";
		status = "okay";
		reg = <0x51>;
		#clock-cells = <0>;
		clock-frequency = <32768>;
		clock-output-names = "hym8563";
		rtc-irq-gpio = <&gpio0 RK_PA6 IRQ_TYPE_EDGE_FALLING>;
	};
};
```

如该设备树所示，将 `rk809` 和 `hym8563` 两个芯片挂载到了 `i2c0` 控制器下

我们也可以阅读硬件原理图，找到我们实际芯片的挂载管脚，并在该管脚所对应的 `i2c` 控制器的设备树节点下加入我们的子设备节点，`

```C
&i2c0 {
	  i2c_device: i2c_device@38 {
          compatible = "i2c_device";
          status = "okay";
          reg = <0x38>;
      };
};
```

#### 实际驱动代码

要驱动一个 `i2c_client` ，需要定义 `i2c_driver`，其定义如下：
```C
struct i2c_driver {
	unsigned int class;

	/* Standard driver model interfaces */
	int (*probe)(struct i2c_client *client, const struct i2c_device_id *id);
	void (*remove)(struct i2c_client *client);

	/* New driver model interface to aid the seamless removal of the
	 * current probe()'s, more commonly unused than used second parameter.
	 */
	int (*probe_new)(struct i2c_client *client);

	/* driver model interfaces that don't relate to enumeration  */
	void (*shutdown)(struct i2c_client *client);

	/* Alert callback, for example for the SMBus alert protocol.
	 * The format and meaning of the data value depends on the protocol.
	 * For the SMBus alert protocol, there is a single bit of data passed
	 * as the alert response's low bit ("event flag").
	 * For the SMBus Host Notify protocol, the data corresponds to the
	 * 16-bit payload data reported by the slave device acting as master.
	 */
	void (*alert)(struct i2c_client *client, enum i2c_alert_protocol protocol,
		      unsigned int data);

	/* a ioctl like command that can be used to perform specific functions
	 * with the device.
	 */
	int (*command)(struct i2c_client *client, unsigned int cmd, void *arg);

	struct device_driver driver;
	const struct i2c_device_id *id_table;

	/* Device detection callback for automatic device creation */
	int (*detect)(struct i2c_client *client, struct i2c_board_info *info);
	const unsigned short *address_list;
	struct list_head clients;

	u32 flags;
};
```

在驱动注册前， `i2c_driver` 结构体需要被正确的初始化，有4个成员是必须的

```C
static struct i2c_driver i2c_device__driver {
    .driver = {
        .name = "i2c_driver",
    },
    .probe = xxx_probe,
    .remove = xxx_remove,
    .id_table = xxx_id_table
};
```

##### 驱动接口函数

1. `i2c_add_driver` 函数

   用于注册一个 **i2c驱动**

   ```C
   int ret = i2c_add_driver(struct i2c_driver *i2c_device__driver);
   ```

2. `i2c_del_driver`函数

   用于删除驱动

   ```C
   int ret = i2c_del_driver(struct i2c_driver *i2c_device__driver);
   ```

3. `i2c_master_send`函数

   用于对 i2c设备 的通用数据发送，表示直接向这个设备塞入`count`个字节

   ```C
   int i2c_master_send(const struct i2c_client, const char *buf, int count);
   ```

   - 例如可以使用以下方式写一个寄存器

     ```C
     u8 buf[2];
     
     buf[0] = 0x02;    // 寄存器
     buf[1] = 0xff;    // 数据
     
     ret = i2c_master_send(client, buf, 2);
     
     /****************************/
     
     static int i2c_write_reg(struct i2c_client *client,
                                 u8 reg, u8 val)
     {
         u8 buf[2] = {
             reg,
             val,
         };
     
         return i2c_master_send(client, buf, 2);
     }
     ```

4. `i2c_master_recv`函数

   用于对 i2c设备 的通用读取，表示直接向这个设备的缓冲区中读出 `count` 个字节

   ```C
   int i2c_master_recv(const struct i2c_client *client, char *buf, int count);
   ```

   但是在实际应用中，单独使用 `i2c_master_recv` 函数并没法很方便的读取寄存器，因为我们得先告诉对端我们要读取哪个寄存器

5. `i2c_smbus_read_byte_data`函数

   用于直接读取一个寄存器的内容

   ```C
   s32 i2c_smbus_read_byte_data(struct i2c_client *client, u8 command);
   ```

   - 使用

     ```C
     int val;
     
     val = i2c_smbus_read_byte_data(client, 0x00);
     
     if (val < 0)
         return val;
     
     dev_info(&client->dev,
              "input = 0x%02x\n",
              val);
     ```

6. `i2c_smbus_write_byte_data`函数

   用于直接写入一个寄存器的内容

   ```C
   i2c_smbus_write_byte_data(client, reg, value);
   ```

   - 使用

     ```C
     i2c_smbus_write_byte_data(client, 0x02, 0xff);
     ```

7. `i2c_transfer`函数

   这是其他通信函数的底层，其他函数都是对该函数的封装

   用于直接发送 `i2c_msg` 信息

   ```C
   int i2c_transfer(struct i2c_adapter *adap,
                    struct i2c_msg *msgs,
                    int num);
   ```

   - 使用：

     ```C
     u8 reg = 0x00;
     u8 val;
     
     struct i2c_msg msgs[2] = {
         // 先发送，我要读什么寄存器
         {
             .addr  = client->addr,
             .flags = 0,
             .len   = 1,
             .buf   = &reg,
         },
         // 取回数据
         {
             .addr  = client->addr,
             .flags = I2C_M_RD,
             .len   = 1,
             .buf   = &val,
         },
     };
     
     ret = i2c_transfer(client->adapter, msgs, 2);
     ```



##### 一个完整实例

```C
#include <linux/module.h>
MODULE_LICENSE("GPL");

#include <linux/i2c.h>
#include <linux/of.h>
#include <linux/of_device.h>

#include <linux/fs.h>
#include <linux/miscdevice.h>

#include <linux/uaccess.h>

/*
&i2c0 {
	  i2c_device: i2c_device@38 {
          compatible = "i2c_device";
          status = "okay";
          reg = <0x38>;
      };
};
*/

// 工具函数，用于读取 i2c 设备的寄存器
static int i2c_read_register(struct i2c_client *client, u8 reg, u8 *val) {
    int ret = i2c_smbus_read_byte_data(client, reg);
    // 如果我们不用 i2c_smbus_read_byte_data，也可以使用原始的 i2c 消息
    struct i2c_msg msgs[2] = {
        {
            .addr = client->addr,
            .flags = 0, // 写操作
            .len = 1,
            .buf = &reg,
        },
        {
            .addr = client->addr,
            .flags = I2C_M_RD, // 读操作
            .len = 1,
            .buf = val,
        }
    };
    i2c_transfer(client->adapter, msgs, 2);
    
    if (ret < 0) {
        printk(KERN_ERR "Failed to read register 0x%02x: %d\n", reg, ret);
        return ret;
    }
    *val = (u8)ret;
    return 0;
}

static int i2c_write_register(struct i2c_client *client, u8 reg, u8 val) {
    int ret = i2c_smbus_write_byte_data(client, reg, val);
    if (ret < 0) {
        printk(KERN_ERR "Failed to write register 0x%02x: %d\n", reg, ret);
        return ret;
    }
    return 0;
}

// 对于应用的不同请求
struct i2c_misc_data {
    struct i2c_client *client;
    struct miscdevice misc_dev;  
};

static int i2c_misc_open(struct inode *inode, struct file *file) {
    // 如果是 miscdevice 的话，file->private_data 会被设置为 miscdevice 结构体的指针
    struct miscdevice *misc_d = file->private_data;
    struct i2c_misc_data *data = container_of(misc_d, struct i2c_misc_data, misc_dev);
    file->private_data = data->client;  // 将 i2c_client 指针存储在 file->private_data 中

    return 0;
}

static int i2c_misc_release(struct inode *inode, struct file *file) {
    return 0;
}

static ssize_t read_i2c_data(struct file *file, char __user *buf, size_t count, loff_t *offset) {
    struct i2c_client *client = file->private_data;
    u8 data[256];
    int ret;

    ret = i2c_master_recv(client, data, count);
    if (ret < 0) {
        printk(KERN_ERR "Failed to read from I2C device: %d\n", ret);
        return ret;
    }

    if (copy_to_user(buf, data, ret)) {
        return -EFAULT;
    }

    return ret;
}

static ssize_t write_i2c_data(struct file *file, const char __user *buf, size_t count, loff_t *offset) {
    struct i2c_client *client = file->private_data;
    u8 data[256];

    if (count > sizeof(data)) {
        return -EINVAL;
    }

    if (copy_from_user(data, buf, count)) {
        return -EFAULT;
    }

    int ret = i2c_master_send(client, data, count);
    if (ret < 0) {
        printk(KERN_ERR "Failed to write to I2C device: %d\n", ret);
        return ret;
    }

    return ret;
}

struct file_operations i2c_fops = {
    .owner = THIS_MODULE,
    .open = i2c_misc_open,
    .release = i2c_misc_release,
    .read = read_i2c_data,
    .write = write_i2c_data,
};



static int i2c_probe(struct i2c_client *client, const struct i2c_device_id *id) {
    printk(KERN_INFO "I2C device probed: %s at address 0x%02x\n", client->name, client->addr);

    struct i2c_misc_data *dev = devm_kzalloc(&client->dev, sizeof(*dev), GFP_KERNEL);
    
    dev->misc_dev.minor = MISC_DYNAMIC_MINOR;
    dev->misc_dev.name = "i2c_misc_device";
    dev->misc_dev.fops = &i2c_fops;
    dev->client = client;
    dev_set_drvdata(&client->dev, dev);

    int ret = misc_register(&(dev->misc_dev));
    if (ret) {
        printk(KERN_ERR "Failed to register I2C misc device: %d\n", ret);
        return ret;
    }


    return 0;
}

static void i2c_remove(struct i2c_client *client) {
    printk(KERN_INFO "I2C device removed: %s at address 0x%02x\n", client->name, client->addr);
    struct i2c_misc_data *dev = dev_get_drvdata(&client->dev);
    misc_deregister(&(dev->misc_dev));
}

struct i2c_driver i2c_driver = {
    .driver = {
        .name = "i2c_driver",
        .owner = THIS_MODULE,
        .of_match_table = (const struct of_device_id[]) {
            { .compatible = "i2c_device" },
            {},
        },
    },
    .probe = i2c_probe,
    .remove = i2c_remove,
    .id_table = (const struct i2c_device_id[]) {
        { "i2c_device", 0 },
        {},
    },
};

static int __init i2c_init(void) {
    printk(KERN_INFO "I2C module initialized\n");

    int ret = i2c_add_driver(&i2c_driver);
    if (ret) {
        printk(KERN_ERR "Failed to register I2C driver: %d\n", ret);
        return ret;
    }

    return 0;
}

static void __exit i2c_exit(void) {
    printk(KERN_INFO "I2C module exited\n");
    i2c_del_driver(&i2c_driver);
}

module_init(i2c_init);
module_exit(i2c_exit);
```



